    <!-- Page Header -->
    <section class="page-header">
        <div class="container">
            <button class="back-btn" onclick="window.location.href='/'">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <line x1="19" y1="12" x2="5" y2="12"></line>
                    <polyline points="12 19 5 12 12 5"></polyline>
                </svg>
                Back to Home
            </button>
            <div class="page-header-content">
                <span class="badge">Testimonials</span>
                <h1 class="page-title">What Our Travelers Say</h1>
                <p class="page-subtitle">Hear from our satisfied adventurers about their unforgettable journeys with
                    WanderLust</p>
            </div>
        </div>
    </section>

    <!-- Loading State -->
    <div id="loadingContainer" class="loading-container" style="padding: 4rem 2rem; text-align: center;">
        <div class="loading-spinner"
            style="width: 50px; height: 50px; border: 4px solid var(--border); border-top-color: var(--primary); border-radius: 50%; animation: spin 1s linear infinite; margin: 0 auto 1rem;">
        </div>
        <p style="color: var(--muted-foreground);">Loading testimonials...</p>
    </div>

    <!-- Error State -->
    <div id="errorContainer" style="display: none;">
        <div class="container">
            <div
                style="background: rgba(255, 111, 97, 0.1); border: 1px solid var(--accent); color: var(--accent); padding: 1.5rem; border-radius: var(--radius-lg); text-align: center; max-width: 600px; margin: 2rem auto;">
                <h3>Unable to load testimonials</h3>
                <p id="errorMessage"></p>
            </div>
        </div>
    </div>

    <!-- Featured Experiences Section -->
    <section id="featuredSection" class="features-section" style="display: none; padding: 3rem 0;">
        <div class="container">
            <div class="section-header scroll-fade-in">
                <h2 class="section-title">Featured Experiences</h2>
            </div>

            <div id="featuredGrid" class="features-grid" style="display: grid; grid-template-columns: 1fr; gap: 2rem;">
                <!-- Featured cards will be dynamically inserted here -->
            </div>
        </div>
    </section>

    <!-- More Traveler Stories Section -->
    <section id="storiesSection" class="destinations-section"
        style="display: none; padding: 4rem 0; background: linear-gradient(135deg, rgba(245, 244, 240, 0.5) 0%, rgba(255, 111, 97, 0.05) 100%);">
        <div class="container">
            <div class="section-header scroll-fade-in">
                <h2 class="section-title">More Traveler Stories</h2>
            </div>

            <div id="storiesGrid" class="destinations-grid">
                <!-- Story cards will be dynamically inserted here -->
            </div>

            <div style="text-align: center; margin-top: 3rem;">
                <button class="btn btn-primary btn-lg"
                    onclick="alert('Load more functionality would show additional reviews from the database')">
                    Load More Reviews
                </button>
            </div>
        </div>
    </section>

    <style>
        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        .testimonial-card {
            background: var(--card);
            border-radius: var(--radius-xl);
            padding: 2rem;
            border: 1px solid var(--border);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            opacity: 0;
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .testimonial-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 32px rgba(0, 0, 0, 0.15);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @media (min-width: 768px) {
            #featuredGrid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>